% todoist

# Run a separate server for debugging
docker-compose run --rm todoist sh -c "python ./scripts/launch_server.py"

# Install ide-tools
pip install doist-ide --upgrade --extra-index https://doist-011833101604.d.codeartifact.us-east-1.amazonaws.com/pypi/doist/simple/
